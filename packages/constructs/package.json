{
  "name": "@pricectl/constructs",
  "version": "0.1.0",
  "description": "Stripe pricing resource constructs for pricectl",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "test": "jest --passWithNoTests",
    "lint": "eslint src --ext .ts"
  },
  "keywords": ["stripe", "iac", "constructs"],
  "license": "MIT",
  "dependencies": {
    "@pricectl/core": "workspace:*",
    "stripe": "^14.9.0"
  },
  "devDependencies": {}
}
